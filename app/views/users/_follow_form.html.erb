<% unless @user.id == current_user.id %>
  <span id="follow_form" class="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </span>
<% end %>