<p id="notice"><%= notice %></p>

<h1>Youtubes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    
  <% @youtubes.each do |youtube| %>
     <div> 
       <%= youtube.channel_title %>
     </div> 
    <div>
      <iframe width="356" height="200" src="https://www.youtube.com/embed/<%= youtube.video_id %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen>
      </iframe>
    </div>
    <div>
      <%= youtube.description%>
    </div>
    <%= link_to "投稿する", new_youtube_path(@youtube) %>
  <% end %>
    <%# @youtube_data.each do |youtube| %>
      <tr>
       <td><%#= link_to 'Show', youtube %></td>
        <td><%#= link_to 'Edit', edit_youtube_path(youtube) %></td>
        <td><%#= link_to 'Destroy', youtube, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>

